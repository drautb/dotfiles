{{ if eq .chezmoi.os "linux" -}}

#!/usr/bin/env bash

# Debian-specific setup

set -eux

sudo apt install -y zsh \
  tmux \
  htop \
  vim \
  git \
  curl \
  wget

if [[ ! "$SHELL" =~ "zsh" ]]; then
  chsh -s "$(command -v zsh)"
fi

{{ end -}}
